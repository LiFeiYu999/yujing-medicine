server:
  port: 8080
spring:
  main:
    allow-bean-definition-overriding=true:
  datasource:
    username: test002
    password: t001Db.3edc578
    url: jdbc:mysql://39.106.155.47:3306/test_yujing_medicine?serverTimezone=Asia/Shanghai&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
#    username: root
#    password: root
#    url: jdbc:mysql://localhost:3306/test_yujing_medicine?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai
#    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  global-config:
    db-config:
      logic-delete-field: is_deleted  # 数据库中的逻辑删除字段名
      logic-delete-value: 1           # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0       # 逻辑未删除值(默认为 0)




